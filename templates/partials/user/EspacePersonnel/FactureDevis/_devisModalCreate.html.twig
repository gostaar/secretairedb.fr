<div class="modal fade" id="factureModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Créer un nouveau Devis</h5>
      </div>
      <div class="modal-body">
        {{ form_start(formViews.addDevis) }}
        
        <div class="d-flex">
            {{ form_row(formViews.addDevis.montant) }}
            {{ form_row(formViews.addDevis.date_devis) }}
            {{ form_row(formViews.addDevis.status) }}
        </div>
        <div class="d-flex">
            {{ form_row(formViews.addDevis.commentaire) }}
            {{ form_row(formViews.addDevis.is_active) }}
        </div>
            <!-- Ajout de la collection factureLignes -->
            {# <div data-controller="form-collection" 
                data-prototype="{{ form_widget(formViews.addDevis.factureLignes.vars.prototype)|e('html_attr') }}">
            </div> #}

        {{ form_end(formViews.addDevis) }}
      </div>
    </div>
  </div>
</div>
